import { Routes } from '@angular/router';
import { PersonajesComponent } from './componens/personajes/personajes.component';

export const routes: Routes = [
    {
        path: '',
        component: PersonajesComponent
    }
];
